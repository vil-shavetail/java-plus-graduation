server:
  port: 8080

spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      discovery:
        locator:
           enabled: true
      routes:
        - id: request-service
          uri: lb://request-service
          predicates:
            - Path=/users/{userId}/requests/**, /users/{userId}/events/{eventId}/requests, /client/request/**

        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/admin/users/**, /client/users/**

        - id: main-service
          uri: lb://main-service
          predicates:
            - Path=/categories/**, /admin/**, /client/**, /users/**, /events/**, /compilations/**

        - id: stats-server
          uri: lb://stats-server
          predicates:
            - Path=/hit/**, /stats/**

management:
  endpoints:
    web:
      exposure:
        include: health, info, mappings, gateway
      base-path: /actuator
  endpoint:
    gateway:
      enabled: true
      routes:
        enabled: true